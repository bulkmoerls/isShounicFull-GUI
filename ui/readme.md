# `ui/` directory

> [!CAUTION]
> Deleting the `.ui` file can render the program **unusable**.

This contains the file responsible for building the UI.

## id for reference

All Labels:

status-label
players-label
map-label
keyword-label

Buttons:

refresh-btn

## faq

But what is the `.cmb` file next to the `.ui` file?
: The `.cmb` file is made using a program called [Cambalache](https://gitlab.gnome.org/jpu/cambalache). The UI is all made using Cambalache, that's why you are seeing this.
